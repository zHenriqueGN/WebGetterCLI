# WebGetterCLI
A simple CLI to get IP addresses and servers from sites


- Running the program
  - "go build -o WG.so"
  - Searching for ips:
    - "./WG.so ip --host [HOST]"
  - Searching for servers:
    - "./WG.so servers --host [HOST]"
